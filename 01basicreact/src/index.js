import React from "react";     //react library
import ReactDOM from "react-dom/client"; //to manipulate the web page
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root")); //react has created its own dom
root.render(<App />);//teeling that ,we have taken reference of the given element